<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.login.mapper.UserMapper">
    <resultMap id="UserResult" type="com.example.login.entity.User">
        <id property="id" column="ID"/>
        <result property="username" column="USERNAME"/>
        <result property="passwordHash" column="PASSWORD_HASH"/>
        <result property="displayName" column="DISPLAY_NAME"/>
        <result property="active" column="ACTIVE"/>
        <result property="lastLoginAt" column="LAST_LOGIN_AT"/>
    </resultMap>

    <select id="findByUsername" parameterType="string" resultMap="UserResult">
        SELECT ID, USERNAME, PASSWORD_HASH, DISPLAY_NAME, ACTIVE, LAST_LOGIN_AT
        FROM APP_USER
        WHERE USERNAME = #{username}
    </select>
</mapper>
